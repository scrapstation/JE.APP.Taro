import { View, Image, Swiper, SwiperItem, Text } from '@tarojs/components';
import Taro from '@tarojs/taro';
import { AtAvatar } from 'taro-ui';
import './index.scss';
const Index: React.FC = () => {
  return (
    <View className='main'>
      <AtAvatar>11</AtAvatar>
    </View>
  );
};

export default Index;
